ome pattern on terrain canvas\"\n    bl_options = {'REGISTER', 'UNDO'}\n    \n    def execute(self, context):\n        try:\n            uv_system = get_uv_canvas_integration()\n            if not uv_system:\n                self.report({'ERROR'}, \"UV Canvas Integration system not available\")\n                return {'CANCELLED'}\n            \n            success = uv_system.create_diagonal_canvas_pattern()\n            \n            if success:\n                self.report({'INFO'}, \"✅ Diagonal pattern created on canvas\")\n            else:\n                self.report({'ERROR'}, \"❌ Failed to create diagonal pattern\")\n                \n        except Exception as e:\n            self.report({'ERROR'}, f\"Pattern creation failed: {str(e)}\")\n            return {'CANCELLED'}\n        \n        return {'FINISHED'}\n\n# Rest of the file continues as written above...\n